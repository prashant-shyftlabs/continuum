"""
Memory Modes Demo - Testing all 4 memory isolation modes.

This demo showcases how the SDK handles different memory isolation modes:
- shared: All memories accessible to all users/agents
- user: Memories isolated per user (shared across sessions for same user)
- agent: Memories isolated per agent (shared across sessions for same agent)
- run: Memories isolated per session/run
"""
